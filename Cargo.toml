[package]
name = "rust-bench-test"
version = "0.1.0"
edition = "2024"

[dependencies]
phf = { version = "0.11.3", features = ["macros"] }

[[bench]]
name = "bench"
harness = false

[dev-dependencies]
criterion = { version = "2.9.1", package = "codspeed-criterion-compat" }
